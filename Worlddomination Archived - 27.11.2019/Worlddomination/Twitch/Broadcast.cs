using System;
using System.Collections.Generic;
using System.Text;

namespace Worlddomination.Twitch
{
    /// <summary>
    /// TO-Do:
    /// create saved config file
    /// that contains the guilds and channels that this class should broadcast to
    /// + contain a copy of last received streams and use that data if its less than an hour old
    /// this class only becomes active if a certain command in guild channels has made an entry into the config
    /// once it is active it acts on calls from the live monitor to if needed broadcast messages in channels 
    /// </summary>
    class Broadcast
    {
    }
}




